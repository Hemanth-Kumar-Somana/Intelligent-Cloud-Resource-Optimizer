 #Google Cloud Project ID where your resources are located and APIs are enabled
# IMPORTANT: Replace 'your-gcp-project-id' with your actual Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud region for Vertex AI and other services (e.g., us-central1, europe-west1)
# This should match the region where you intend to use Vertex AI and where your resources are.
GOOGLE_CLOUD_LOCATION=us-central1

# This is CRUCIAL for ADK to use Gemini models via Vertex AI
# Do NOT change this unless you have a specific reason (e.g., using a different model endpoint).
GOOGLE_GENAI_USE_VERTEXAI=True

# --- Optional Configurations (Examples) ---
# You can add any other non-sensitive configurations specific to your agents here.
# For example, thresholds for optimization, or specific policy rules.
# OPTIMIZATION_THRESHOLD_CPU_PERCENT=15  # Example: Flag VMs if CPU utilization is below 15%
# POLICY_MIN_VM_VCPU=2                  # Example: Minimum vCPUs allowed for "production" VMs
# DATA_RETRIEVAL_DAYS=7   
